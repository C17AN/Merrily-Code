import React from "react";

interface HighlightedTextProps {
  color: string;
  backgroundColor: string;
}

const HighlightedText = ({ color, backgroundColor }: HighlightedTextProps) => {
  return <div></div>;
};

export default HighlightedText;
